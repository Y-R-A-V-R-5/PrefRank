# PrefRank – GPU  (Python 3.10+, CUDA 11.8+)
# pip install -r requirements_gpu.txt --extra-index-url https://download.pytorch.org/whl/cu118

numpy>=1.26,<2.0
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0                # GPU: XGBClassifier(..., device="cuda")
lightgbm>=4.3               # GPU: LGBMClassifier(..., device="gpu")
torch>=2.2                  # install CUDA wheel via --extra-index-url above
pyyaml>=6.0
tqdm>=4.66
textstat>=0.7
scipy>=1.13
matplotlib>=3.8
seaborn>=0.13
joblib>=1.4
